# claude-auto-retry wraps Claude in tmux. A Cursor terminal resize often
# leaves the TUI stuck on an old pane width. Reload: tmux source-file ~/.tmux.conf
setw -g aggressive-resize on
setw -g window-size latest
